<h3><span class="bg-secondary px-3">ジャンル一覧・追加</span></h3>

<%= form_with model: @genre, url: admin_genres_path, local:true do |f| %>

<p>ジャンル名</p>
<%= f.text_field :name %>
<%= f.submit "新規登録" ,class: "btn btn-primary"%>



　<table class="table">
　    <thead class="table-secondary">
  　       <th>ジャンル</th>
  　       <th></th>
　    </thead>
　    <tbody>
　       <% @genres.each do |genre| %>
    　     <tr>
    　       <td class="col-m-4"><%= genre.name %></td>
    　       <td class="col-md-8"><%= link_to "編集する", edit_admin_genre_path(genre.id), class: "btn btn-primary" %></td>
    　     </t>

　       <% end %>
　    </tbody>
　</table>
<% end %>